# Changelog

## 2026.5.10

- Initial dev channel.
- Pins `socialhome` with `pip3 install --pre` so the dev add-on
  can ride ahead of stable on a CalVer pre-release (e.g.
  `2026.5.11rc1`) before it's promoted.
- Same options + behaviour as the stable add-on; database and
  federation formats may shift between pre-releases.
